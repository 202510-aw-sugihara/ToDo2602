<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todo.TodoRepository">
  <select id="searchByTitle" parameterType="string" resultType="com.example.todo.Todo">
    SELECT
      id,
      author,
      title,
      description,
      due_date,
      priority,
      completed,
      created_at,
      updated_at,
      version
    FROM todos
    WHERE title LIKE CONCAT('%', #{keyword}, '%')
    ORDER BY created_at DESC
  </select>
</mapper>
